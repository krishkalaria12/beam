{
  "$schema": "https://raw.githubusercontent.com/krishkalaria12/beam/main/resources/extra/schemas/extension.json",
  "name": "beam-utils-lab",
  "title": "Beam Utils Lab",
  "description": "A Beam-native playground for @beam-launcher/api and @beam-launcher/utils.",
  "icon": "assets/icon.svg",
  "author": "beam-launcher",
  "owner": "beam-launcher",
  "license": "MIT",
  "version": "0.1.0",
  "commands": [
    {
      "name": "dashboard",
      "title": "Beam Utils Dashboard",
      "description": "Inspect Beam package signals, local tooling, and internal deeplinks with Beam utils.",
      "mode": "view"
    },
    {
      "name": "workflow-form",
      "title": "Beam Workflow Form",
      "description": "Compose a reusable Beam workflow draft with Beam utils form helpers.",
      "mode": "view"
    },
    {
      "name": "capture-snapshot",
      "title": "Capture Beam Snapshot",
      "description": "Copy a Beam runtime snapshot with cached package signals and current desktop context.",
      "mode": "no-view"
    }
  ],
  "preferences": [
    {
      "name": "defaultQuery",
      "title": "Default Query",
      "description": "Starter filter for the package dashboard.",
      "type": "textfield",
      "placeholder": "beam",
      "required": false,
      "default": "beam"
    },
    {
      "name": "preferredOpenTarget",
      "title": "Preferred Open Target",
      "description": "Choose whether package actions open npm or repository pages first.",
      "type": "dropdown",
      "required": false,
      "default": "repository",
      "data": [
        {
          "title": "Repository",
          "value": "repository"
        },
        {
          "title": "npm Package",
          "value": "npm"
        }
      ]
    },
    {
      "name": "showRepositorySignals",
      "title": "Repository Signals",
      "description": "Include Beam GitHub repository stats in the dashboard status card.",
      "type": "checkbox",
      "label": "Show Beam repository metrics",
      "required": false,
      "default": true
    }
  ],
  "scripts": {
    "build": "node ../../../packages/beam-api/bin/run.js build --src . --out /home/krish/.local/share/com.tauri.dev/plugins/beam-utils-lab",
    "dev": "node ../../../packages/beam-api/bin/run.js develop --target .",
    "check": "bunx tsc --noEmit",
    "format": "biome format --write src"
  },
  "dependencies": {
    "@beam-launcher/api": "^1.0.0",
    "@beam-launcher/utils": "^0.1.0",
    "react": "^19.2.3"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.2",
    "@types/node": "^24.9.2",
    "@types/react": "^19.2.2",
    "typescript": "^5.9.2"
  }
}

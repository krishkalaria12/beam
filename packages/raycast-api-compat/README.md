# `@beam/raycast-api-compat`

Thin compatibility package that maps Raycast-style imports onto Beam's native
SDK surface.

This is the package boundary for Beam's Raycast compatibility layer. Runtime
behavior is still owned by the extension manager while the public export surface
is migrated here incrementally.

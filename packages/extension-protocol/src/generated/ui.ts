// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v6.33.1
// source: ui.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  AlertActionStyle,
  alertActionStyleFromJSON,
  alertActionStyleToJSON,
  PopToRootType,
  popToRootTypeFromJSON,
  popToRootTypeToJSON,
  ToastStyle,
  toastStyleFromJSON,
  toastStyleToJSON,
} from "./common";

export interface ToastAction {
  title: string;
  callbackId: string;
}

export interface ShowToastRequest {
  id: number;
  style: ToastStyle;
  title: string;
  message: string;
  primaryAction: ToastAction | undefined;
  secondaryAction: ToastAction | undefined;
}

export interface ShowToastResponse {
  ok: boolean;
}

export interface UpdateToastRequest {
  id: number;
  style: ToastStyle;
  title: string;
  message: string;
}

export interface UpdateToastResponse {
  ok: boolean;
}

export interface HideToastRequest {
  id: number;
}

export interface HideToastResponse {
  ok: boolean;
}

export interface ShowHudRequest {
  text: string;
  clearRootSearch: boolean;
  popToRoot: PopToRootType;
}

export interface ShowHudResponse {
  ok: boolean;
}

export interface ConfirmAlertAction {
  title: string;
  style: AlertActionStyle;
}

export interface ConfirmAlertRequest {
  title: string;
  message: string;
  primaryAction: ConfirmAlertAction | undefined;
  dismissAction: ConfirmAlertAction | undefined;
}

export interface ConfirmAlertResponse {
  confirmed: boolean;
}

export interface CloseMainWindowRequest {
  clearRootSearch: boolean;
  popToRoot: PopToRootType;
}

export interface CloseMainWindowResponse {
  ok: boolean;
}

export interface SetSearchTextRequest {
  text: string;
}

export interface SetSearchTextResponse {
  ok: boolean;
}

export interface GetSelectedTextRequest {
}

export interface GetSelectedTextResponse {
  text: string;
}

export interface PopToRootRequest {
  clearSearchBar: boolean;
}

export interface PopToRootResponse {
  ok: boolean;
}

export interface PushViewRequest {
}

export interface PushViewResponse {
  ok: boolean;
}

export interface PopViewRequest {
}

export interface PopViewResponse {
  ok: boolean;
}

export interface UpdateCommandMetadataRequest {
  subtitle: string;
}

export interface UpdateCommandMetadataResponse {
  ok: boolean;
}

function createBaseToastAction(): ToastAction {
  return { title: "", callbackId: "" };
}

export const ToastAction: MessageFns<ToastAction> = {
  encode(message: ToastAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.title !== "") {
      writer.uint32(10).string(message.title);
    }
    if (message.callbackId !== "") {
      writer.uint32(18).string(message.callbackId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToastAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToastAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.callbackId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToastAction {
    return {
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      callbackId: isSet(object.callbackId)
        ? globalThis.String(object.callbackId)
        : isSet(object.callback_id)
        ? globalThis.String(object.callback_id)
        : "",
    };
  },

  toJSON(message: ToastAction): unknown {
    const obj: any = {};
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.callbackId !== "") {
      obj.callbackId = message.callbackId;
    }
    return obj;
  },

  create(base?: DeepPartial<ToastAction>): ToastAction {
    return ToastAction.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ToastAction>): ToastAction {
    const message = createBaseToastAction();
    message.title = object.title ?? "";
    message.callbackId = object.callbackId ?? "";
    return message;
  },
};

function createBaseShowToastRequest(): ShowToastRequest {
  return { id: 0, style: 0, title: "", message: "", primaryAction: undefined, secondaryAction: undefined };
}

export const ShowToastRequest: MessageFns<ShowToastRequest> = {
  encode(message: ShowToastRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.style !== 0) {
      writer.uint32(16).int32(message.style);
    }
    if (message.title !== "") {
      writer.uint32(26).string(message.title);
    }
    if (message.message !== "") {
      writer.uint32(34).string(message.message);
    }
    if (message.primaryAction !== undefined) {
      ToastAction.encode(message.primaryAction, writer.uint32(42).fork()).join();
    }
    if (message.secondaryAction !== undefined) {
      ToastAction.encode(message.secondaryAction, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShowToastRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShowToastRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.style = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.primaryAction = ToastAction.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.secondaryAction = ToastAction.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ShowToastRequest {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      style: isSet(object.style) ? toastStyleFromJSON(object.style) : 0,
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      primaryAction: isSet(object.primaryAction)
        ? ToastAction.fromJSON(object.primaryAction)
        : isSet(object.primary_action)
        ? ToastAction.fromJSON(object.primary_action)
        : undefined,
      secondaryAction: isSet(object.secondaryAction)
        ? ToastAction.fromJSON(object.secondaryAction)
        : isSet(object.secondary_action)
        ? ToastAction.fromJSON(object.secondary_action)
        : undefined,
    };
  },

  toJSON(message: ShowToastRequest): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.style !== 0) {
      obj.style = toastStyleToJSON(message.style);
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.primaryAction !== undefined) {
      obj.primaryAction = ToastAction.toJSON(message.primaryAction);
    }
    if (message.secondaryAction !== undefined) {
      obj.secondaryAction = ToastAction.toJSON(message.secondaryAction);
    }
    return obj;
  },

  create(base?: DeepPartial<ShowToastRequest>): ShowToastRequest {
    return ShowToastRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ShowToastRequest>): ShowToastRequest {
    const message = createBaseShowToastRequest();
    message.id = object.id ?? 0;
    message.style = object.style ?? 0;
    message.title = object.title ?? "";
    message.message = object.message ?? "";
    message.primaryAction = (object.primaryAction !== undefined && object.primaryAction !== null)
      ? ToastAction.fromPartial(object.primaryAction)
      : undefined;
    message.secondaryAction = (object.secondaryAction !== undefined && object.secondaryAction !== null)
      ? ToastAction.fromPartial(object.secondaryAction)
      : undefined;
    return message;
  },
};

function createBaseShowToastResponse(): ShowToastResponse {
  return { ok: false };
}

export const ShowToastResponse: MessageFns<ShowToastResponse> = {
  encode(message: ShowToastResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShowToastResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShowToastResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ShowToastResponse {
    return { ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false };
  },

  toJSON(message: ShowToastResponse): unknown {
    const obj: any = {};
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    return obj;
  },

  create(base?: DeepPartial<ShowToastResponse>): ShowToastResponse {
    return ShowToastResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ShowToastResponse>): ShowToastResponse {
    const message = createBaseShowToastResponse();
    message.ok = object.ok ?? false;
    return message;
  },
};

function createBaseUpdateToastRequest(): UpdateToastRequest {
  return { id: 0, style: 0, title: "", message: "" };
}

export const UpdateToastRequest: MessageFns<UpdateToastRequest> = {
  encode(message: UpdateToastRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.style !== 0) {
      writer.uint32(16).int32(message.style);
    }
    if (message.title !== "") {
      writer.uint32(26).string(message.title);
    }
    if (message.message !== "") {
      writer.uint32(34).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateToastRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateToastRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.style = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateToastRequest {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      style: isSet(object.style) ? toastStyleFromJSON(object.style) : 0,
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: UpdateToastRequest): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.style !== 0) {
      obj.style = toastStyleToJSON(message.style);
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<UpdateToastRequest>): UpdateToastRequest {
    return UpdateToastRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpdateToastRequest>): UpdateToastRequest {
    const message = createBaseUpdateToastRequest();
    message.id = object.id ?? 0;
    message.style = object.style ?? 0;
    message.title = object.title ?? "";
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseUpdateToastResponse(): UpdateToastResponse {
  return { ok: false };
}

export const UpdateToastResponse: MessageFns<UpdateToastResponse> = {
  encode(message: UpdateToastResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateToastResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateToastResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateToastResponse {
    return { ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false };
  },

  toJSON(message: UpdateToastResponse): unknown {
    const obj: any = {};
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    return obj;
  },

  create(base?: DeepPartial<UpdateToastResponse>): UpdateToastResponse {
    return UpdateToastResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpdateToastResponse>): UpdateToastResponse {
    const message = createBaseUpdateToastResponse();
    message.ok = object.ok ?? false;
    return message;
  },
};

function createBaseHideToastRequest(): HideToastRequest {
  return { id: 0 };
}

export const HideToastRequest: MessageFns<HideToastRequest> = {
  encode(message: HideToastRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HideToastRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHideToastRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HideToastRequest {
    return { id: isSet(object.id) ? globalThis.Number(object.id) : 0 };
  },

  toJSON(message: HideToastRequest): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    return obj;
  },

  create(base?: DeepPartial<HideToastRequest>): HideToastRequest {
    return HideToastRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HideToastRequest>): HideToastRequest {
    const message = createBaseHideToastRequest();
    message.id = object.id ?? 0;
    return message;
  },
};

function createBaseHideToastResponse(): HideToastResponse {
  return { ok: false };
}

export const HideToastResponse: MessageFns<HideToastResponse> = {
  encode(message: HideToastResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HideToastResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHideToastResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HideToastResponse {
    return { ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false };
  },

  toJSON(message: HideToastResponse): unknown {
    const obj: any = {};
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    return obj;
  },

  create(base?: DeepPartial<HideToastResponse>): HideToastResponse {
    return HideToastResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HideToastResponse>): HideToastResponse {
    const message = createBaseHideToastResponse();
    message.ok = object.ok ?? false;
    return message;
  },
};

function createBaseShowHudRequest(): ShowHudRequest {
  return { text: "", clearRootSearch: false, popToRoot: 0 };
}

export const ShowHudRequest: MessageFns<ShowHudRequest> = {
  encode(message: ShowHudRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.clearRootSearch !== false) {
      writer.uint32(16).bool(message.clearRootSearch);
    }
    if (message.popToRoot !== 0) {
      writer.uint32(24).int32(message.popToRoot);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShowHudRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShowHudRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.clearRootSearch = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.popToRoot = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ShowHudRequest {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      clearRootSearch: isSet(object.clearRootSearch)
        ? globalThis.Boolean(object.clearRootSearch)
        : isSet(object.clear_root_search)
        ? globalThis.Boolean(object.clear_root_search)
        : false,
      popToRoot: isSet(object.popToRoot)
        ? popToRootTypeFromJSON(object.popToRoot)
        : isSet(object.pop_to_root)
        ? popToRootTypeFromJSON(object.pop_to_root)
        : 0,
    };
  },

  toJSON(message: ShowHudRequest): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.clearRootSearch !== false) {
      obj.clearRootSearch = message.clearRootSearch;
    }
    if (message.popToRoot !== 0) {
      obj.popToRoot = popToRootTypeToJSON(message.popToRoot);
    }
    return obj;
  },

  create(base?: DeepPartial<ShowHudRequest>): ShowHudRequest {
    return ShowHudRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ShowHudRequest>): ShowHudRequest {
    const message = createBaseShowHudRequest();
    message.text = object.text ?? "";
    message.clearRootSearch = object.clearRootSearch ?? false;
    message.popToRoot = object.popToRoot ?? 0;
    return message;
  },
};

function createBaseShowHudResponse(): ShowHudResponse {
  return { ok: false };
}

export const ShowHudResponse: MessageFns<ShowHudResponse> = {
  encode(message: ShowHudResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShowHudResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShowHudResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ShowHudResponse {
    return { ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false };
  },

  toJSON(message: ShowHudResponse): unknown {
    const obj: any = {};
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    return obj;
  },

  create(base?: DeepPartial<ShowHudResponse>): ShowHudResponse {
    return ShowHudResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ShowHudResponse>): ShowHudResponse {
    const message = createBaseShowHudResponse();
    message.ok = object.ok ?? false;
    return message;
  },
};

function createBaseConfirmAlertAction(): ConfirmAlertAction {
  return { title: "", style: 0 };
}

export const ConfirmAlertAction: MessageFns<ConfirmAlertAction> = {
  encode(message: ConfirmAlertAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.title !== "") {
      writer.uint32(10).string(message.title);
    }
    if (message.style !== 0) {
      writer.uint32(16).int32(message.style);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfirmAlertAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfirmAlertAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.style = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConfirmAlertAction {
    return {
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      style: isSet(object.style) ? alertActionStyleFromJSON(object.style) : 0,
    };
  },

  toJSON(message: ConfirmAlertAction): unknown {
    const obj: any = {};
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.style !== 0) {
      obj.style = alertActionStyleToJSON(message.style);
    }
    return obj;
  },

  create(base?: DeepPartial<ConfirmAlertAction>): ConfirmAlertAction {
    return ConfirmAlertAction.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ConfirmAlertAction>): ConfirmAlertAction {
    const message = createBaseConfirmAlertAction();
    message.title = object.title ?? "";
    message.style = object.style ?? 0;
    return message;
  },
};

function createBaseConfirmAlertRequest(): ConfirmAlertRequest {
  return { title: "", message: "", primaryAction: undefined, dismissAction: undefined };
}

export const ConfirmAlertRequest: MessageFns<ConfirmAlertRequest> = {
  encode(message: ConfirmAlertRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.title !== "") {
      writer.uint32(10).string(message.title);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    if (message.primaryAction !== undefined) {
      ConfirmAlertAction.encode(message.primaryAction, writer.uint32(26).fork()).join();
    }
    if (message.dismissAction !== undefined) {
      ConfirmAlertAction.encode(message.dismissAction, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfirmAlertRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfirmAlertRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.primaryAction = ConfirmAlertAction.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.dismissAction = ConfirmAlertAction.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConfirmAlertRequest {
    return {
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      primaryAction: isSet(object.primaryAction)
        ? ConfirmAlertAction.fromJSON(object.primaryAction)
        : isSet(object.primary_action)
        ? ConfirmAlertAction.fromJSON(object.primary_action)
        : undefined,
      dismissAction: isSet(object.dismissAction)
        ? ConfirmAlertAction.fromJSON(object.dismissAction)
        : isSet(object.dismiss_action)
        ? ConfirmAlertAction.fromJSON(object.dismiss_action)
        : undefined,
    };
  },

  toJSON(message: ConfirmAlertRequest): unknown {
    const obj: any = {};
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.primaryAction !== undefined) {
      obj.primaryAction = ConfirmAlertAction.toJSON(message.primaryAction);
    }
    if (message.dismissAction !== undefined) {
      obj.dismissAction = ConfirmAlertAction.toJSON(message.dismissAction);
    }
    return obj;
  },

  create(base?: DeepPartial<ConfirmAlertRequest>): ConfirmAlertRequest {
    return ConfirmAlertRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ConfirmAlertRequest>): ConfirmAlertRequest {
    const message = createBaseConfirmAlertRequest();
    message.title = object.title ?? "";
    message.message = object.message ?? "";
    message.primaryAction = (object.primaryAction !== undefined && object.primaryAction !== null)
      ? ConfirmAlertAction.fromPartial(object.primaryAction)
      : undefined;
    message.dismissAction = (object.dismissAction !== undefined && object.dismissAction !== null)
      ? ConfirmAlertAction.fromPartial(object.dismissAction)
      : undefined;
    return message;
  },
};

function createBaseConfirmAlertResponse(): ConfirmAlertResponse {
  return { confirmed: false };
}

export const ConfirmAlertResponse: MessageFns<ConfirmAlertResponse> = {
  encode(message: ConfirmAlertResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.confirmed !== false) {
      writer.uint32(8).bool(message.confirmed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfirmAlertResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfirmAlertResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.confirmed = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConfirmAlertResponse {
    return { confirmed: isSet(object.confirmed) ? globalThis.Boolean(object.confirmed) : false };
  },

  toJSON(message: ConfirmAlertResponse): unknown {
    const obj: any = {};
    if (message.confirmed !== false) {
      obj.confirmed = message.confirmed;
    }
    return obj;
  },

  create(base?: DeepPartial<ConfirmAlertResponse>): ConfirmAlertResponse {
    return ConfirmAlertResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ConfirmAlertResponse>): ConfirmAlertResponse {
    const message = createBaseConfirmAlertResponse();
    message.confirmed = object.confirmed ?? false;
    return message;
  },
};

function createBaseCloseMainWindowRequest(): CloseMainWindowRequest {
  return { clearRootSearch: false, popToRoot: 0 };
}

export const CloseMainWindowRequest: MessageFns<CloseMainWindowRequest> = {
  encode(message: CloseMainWindowRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.clearRootSearch !== false) {
      writer.uint32(8).bool(message.clearRootSearch);
    }
    if (message.popToRoot !== 0) {
      writer.uint32(16).int32(message.popToRoot);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CloseMainWindowRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCloseMainWindowRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.clearRootSearch = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.popToRoot = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CloseMainWindowRequest {
    return {
      clearRootSearch: isSet(object.clearRootSearch)
        ? globalThis.Boolean(object.clearRootSearch)
        : isSet(object.clear_root_search)
        ? globalThis.Boolean(object.clear_root_search)
        : false,
      popToRoot: isSet(object.popToRoot)
        ? popToRootTypeFromJSON(object.popToRoot)
        : isSet(object.pop_to_root)
        ? popToRootTypeFromJSON(object.pop_to_root)
        : 0,
    };
  },

  toJSON(message: CloseMainWindowRequest): unknown {
    const obj: any = {};
    if (message.clearRootSearch !== false) {
      obj.clearRootSearch = message.clearRootSearch;
    }
    if (message.popToRoot !== 0) {
      obj.popToRoot = popToRootTypeToJSON(message.popToRoot);
    }
    return obj;
  },

  create(base?: DeepPartial<CloseMainWindowRequest>): CloseMainWindowRequest {
    return CloseMainWindowRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CloseMainWindowRequest>): CloseMainWindowRequest {
    const message = createBaseCloseMainWindowRequest();
    message.clearRootSearch = object.clearRootSearch ?? false;
    message.popToRoot = object.popToRoot ?? 0;
    return message;
  },
};

function createBaseCloseMainWindowResponse(): CloseMainWindowResponse {
  return { ok: false };
}

export const CloseMainWindowResponse: MessageFns<CloseMainWindowResponse> = {
  encode(message: CloseMainWindowResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CloseMainWindowResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCloseMainWindowResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CloseMainWindowResponse {
    return { ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false };
  },

  toJSON(message: CloseMainWindowResponse): unknown {
    const obj: any = {};
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    return obj;
  },

  create(base?: DeepPartial<CloseMainWindowResponse>): CloseMainWindowResponse {
    return CloseMainWindowResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CloseMainWindowResponse>): CloseMainWindowResponse {
    const message = createBaseCloseMainWindowResponse();
    message.ok = object.ok ?? false;
    return message;
  },
};

function createBaseSetSearchTextRequest(): SetSearchTextRequest {
  return { text: "" };
}

export const SetSearchTextRequest: MessageFns<SetSearchTextRequest> = {
  encode(message: SetSearchTextRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetSearchTextRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetSearchTextRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetSearchTextRequest {
    return { text: isSet(object.text) ? globalThis.String(object.text) : "" };
  },

  toJSON(message: SetSearchTextRequest): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    return obj;
  },

  create(base?: DeepPartial<SetSearchTextRequest>): SetSearchTextRequest {
    return SetSearchTextRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetSearchTextRequest>): SetSearchTextRequest {
    const message = createBaseSetSearchTextRequest();
    message.text = object.text ?? "";
    return message;
  },
};

function createBaseSetSearchTextResponse(): SetSearchTextResponse {
  return { ok: false };
}

export const SetSearchTextResponse: MessageFns<SetSearchTextResponse> = {
  encode(message: SetSearchTextResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetSearchTextResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetSearchTextResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetSearchTextResponse {
    return { ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false };
  },

  toJSON(message: SetSearchTextResponse): unknown {
    const obj: any = {};
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    return obj;
  },

  create(base?: DeepPartial<SetSearchTextResponse>): SetSearchTextResponse {
    return SetSearchTextResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetSearchTextResponse>): SetSearchTextResponse {
    const message = createBaseSetSearchTextResponse();
    message.ok = object.ok ?? false;
    return message;
  },
};

function createBaseGetSelectedTextRequest(): GetSelectedTextRequest {
  return {};
}

export const GetSelectedTextRequest: MessageFns<GetSelectedTextRequest> = {
  encode(_: GetSelectedTextRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSelectedTextRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSelectedTextRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetSelectedTextRequest {
    return {};
  },

  toJSON(_: GetSelectedTextRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<GetSelectedTextRequest>): GetSelectedTextRequest {
    return GetSelectedTextRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<GetSelectedTextRequest>): GetSelectedTextRequest {
    const message = createBaseGetSelectedTextRequest();
    return message;
  },
};

function createBaseGetSelectedTextResponse(): GetSelectedTextResponse {
  return { text: "" };
}

export const GetSelectedTextResponse: MessageFns<GetSelectedTextResponse> = {
  encode(message: GetSelectedTextResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSelectedTextResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSelectedTextResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSelectedTextResponse {
    return { text: isSet(object.text) ? globalThis.String(object.text) : "" };
  },

  toJSON(message: GetSelectedTextResponse): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    return obj;
  },

  create(base?: DeepPartial<GetSelectedTextResponse>): GetSelectedTextResponse {
    return GetSelectedTextResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetSelectedTextResponse>): GetSelectedTextResponse {
    const message = createBaseGetSelectedTextResponse();
    message.text = object.text ?? "";
    return message;
  },
};

function createBasePopToRootRequest(): PopToRootRequest {
  return { clearSearchBar: false };
}

export const PopToRootRequest: MessageFns<PopToRootRequest> = {
  encode(message: PopToRootRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.clearSearchBar !== false) {
      writer.uint32(8).bool(message.clearSearchBar);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PopToRootRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePopToRootRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.clearSearchBar = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PopToRootRequest {
    return {
      clearSearchBar: isSet(object.clearSearchBar)
        ? globalThis.Boolean(object.clearSearchBar)
        : isSet(object.clear_search_bar)
        ? globalThis.Boolean(object.clear_search_bar)
        : false,
    };
  },

  toJSON(message: PopToRootRequest): unknown {
    const obj: any = {};
    if (message.clearSearchBar !== false) {
      obj.clearSearchBar = message.clearSearchBar;
    }
    return obj;
  },

  create(base?: DeepPartial<PopToRootRequest>): PopToRootRequest {
    return PopToRootRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PopToRootRequest>): PopToRootRequest {
    const message = createBasePopToRootRequest();
    message.clearSearchBar = object.clearSearchBar ?? false;
    return message;
  },
};

function createBasePopToRootResponse(): PopToRootResponse {
  return { ok: false };
}

export const PopToRootResponse: MessageFns<PopToRootResponse> = {
  encode(message: PopToRootResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PopToRootResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePopToRootResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PopToRootResponse {
    return { ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false };
  },

  toJSON(message: PopToRootResponse): unknown {
    const obj: any = {};
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    return obj;
  },

  create(base?: DeepPartial<PopToRootResponse>): PopToRootResponse {
    return PopToRootResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PopToRootResponse>): PopToRootResponse {
    const message = createBasePopToRootResponse();
    message.ok = object.ok ?? false;
    return message;
  },
};

function createBasePushViewRequest(): PushViewRequest {
  return {};
}

export const PushViewRequest: MessageFns<PushViewRequest> = {
  encode(_: PushViewRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushViewRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushViewRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): PushViewRequest {
    return {};
  },

  toJSON(_: PushViewRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<PushViewRequest>): PushViewRequest {
    return PushViewRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<PushViewRequest>): PushViewRequest {
    const message = createBasePushViewRequest();
    return message;
  },
};

function createBasePushViewResponse(): PushViewResponse {
  return { ok: false };
}

export const PushViewResponse: MessageFns<PushViewResponse> = {
  encode(message: PushViewResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushViewResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushViewResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PushViewResponse {
    return { ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false };
  },

  toJSON(message: PushViewResponse): unknown {
    const obj: any = {};
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    return obj;
  },

  create(base?: DeepPartial<PushViewResponse>): PushViewResponse {
    return PushViewResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PushViewResponse>): PushViewResponse {
    const message = createBasePushViewResponse();
    message.ok = object.ok ?? false;
    return message;
  },
};

function createBasePopViewRequest(): PopViewRequest {
  return {};
}

export const PopViewRequest: MessageFns<PopViewRequest> = {
  encode(_: PopViewRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PopViewRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePopViewRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): PopViewRequest {
    return {};
  },

  toJSON(_: PopViewRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<PopViewRequest>): PopViewRequest {
    return PopViewRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<PopViewRequest>): PopViewRequest {
    const message = createBasePopViewRequest();
    return message;
  },
};

function createBasePopViewResponse(): PopViewResponse {
  return { ok: false };
}

export const PopViewResponse: MessageFns<PopViewResponse> = {
  encode(message: PopViewResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PopViewResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePopViewResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PopViewResponse {
    return { ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false };
  },

  toJSON(message: PopViewResponse): unknown {
    const obj: any = {};
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    return obj;
  },

  create(base?: DeepPartial<PopViewResponse>): PopViewResponse {
    return PopViewResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PopViewResponse>): PopViewResponse {
    const message = createBasePopViewResponse();
    message.ok = object.ok ?? false;
    return message;
  },
};

function createBaseUpdateCommandMetadataRequest(): UpdateCommandMetadataRequest {
  return { subtitle: "" };
}

export const UpdateCommandMetadataRequest: MessageFns<UpdateCommandMetadataRequest> = {
  encode(message: UpdateCommandMetadataRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.subtitle !== "") {
      writer.uint32(10).string(message.subtitle);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateCommandMetadataRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateCommandMetadataRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.subtitle = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateCommandMetadataRequest {
    return { subtitle: isSet(object.subtitle) ? globalThis.String(object.subtitle) : "" };
  },

  toJSON(message: UpdateCommandMetadataRequest): unknown {
    const obj: any = {};
    if (message.subtitle !== "") {
      obj.subtitle = message.subtitle;
    }
    return obj;
  },

  create(base?: DeepPartial<UpdateCommandMetadataRequest>): UpdateCommandMetadataRequest {
    return UpdateCommandMetadataRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpdateCommandMetadataRequest>): UpdateCommandMetadataRequest {
    const message = createBaseUpdateCommandMetadataRequest();
    message.subtitle = object.subtitle ?? "";
    return message;
  },
};

function createBaseUpdateCommandMetadataResponse(): UpdateCommandMetadataResponse {
  return { ok: false };
}

export const UpdateCommandMetadataResponse: MessageFns<UpdateCommandMetadataResponse> = {
  encode(message: UpdateCommandMetadataResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateCommandMetadataResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateCommandMetadataResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateCommandMetadataResponse {
    return { ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false };
  },

  toJSON(message: UpdateCommandMetadataResponse): unknown {
    const obj: any = {};
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    return obj;
  },

  create(base?: DeepPartial<UpdateCommandMetadataResponse>): UpdateCommandMetadataResponse {
    return UpdateCommandMetadataResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpdateCommandMetadataResponse>): UpdateCommandMetadataResponse {
    const message = createBaseUpdateCommandMetadataResponse();
    message.ok = object.ok ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

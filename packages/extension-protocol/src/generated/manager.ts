// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v6.33.1
// source: manager.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { DesktopContextSnapshot, EnvironmentSnapshot } from "./environment";
import { Struct } from "./google/protobuf/struct";

export interface RuntimeLaunchPayload {
  extensionId: string;
  extensionPath: string;
  entrypointPath: string;
  environment: EnvironmentSnapshot | undefined;
  desktopContext: DesktopContextSnapshot | undefined;
  preferenceValues: { [key: string]: any } | undefined;
  launchArguments: { [key: string]: any } | undefined;
  launchContext: { [key: string]: any } | undefined;
  fallbackText: string;
}

export interface PingRequest {
}

export interface PingResponse {
  ok: boolean;
}

export interface StartDevSessionRequest {
  extensionId: string;
}

export interface StartDevSessionResponse {
  ok: boolean;
}

export interface RefreshDevSessionRequest {
  extensionId: string;
}

export interface RefreshDevSessionResponse {
  ok: boolean;
}

export interface StopDevSessionRequest {
  extensionId: string;
}

export interface StopDevSessionResponse {
  ok: boolean;
}

export interface ShutdownEvent {
}

export interface PopViewEvent {
}

export interface PreferencesChangedEvent {
  values: { [key: string]: any } | undefined;
}

export interface RuntimeEvent {
  shutdown?: ShutdownEvent | undefined;
  popView?: PopViewEvent | undefined;
  preferencesChanged?: PreferencesChangedEvent | undefined;
}

function createBaseRuntimeLaunchPayload(): RuntimeLaunchPayload {
  return {
    extensionId: "",
    extensionPath: "",
    entrypointPath: "",
    environment: undefined,
    desktopContext: undefined,
    preferenceValues: undefined,
    launchArguments: undefined,
    launchContext: undefined,
    fallbackText: "",
  };
}

export const RuntimeLaunchPayload: MessageFns<RuntimeLaunchPayload> = {
  encode(message: RuntimeLaunchPayload, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.extensionId !== "") {
      writer.uint32(10).string(message.extensionId);
    }
    if (message.extensionPath !== "") {
      writer.uint32(18).string(message.extensionPath);
    }
    if (message.entrypointPath !== "") {
      writer.uint32(26).string(message.entrypointPath);
    }
    if (message.environment !== undefined) {
      EnvironmentSnapshot.encode(message.environment, writer.uint32(34).fork()).join();
    }
    if (message.desktopContext !== undefined) {
      DesktopContextSnapshot.encode(message.desktopContext, writer.uint32(42).fork()).join();
    }
    if (message.preferenceValues !== undefined) {
      Struct.encode(Struct.wrap(message.preferenceValues), writer.uint32(50).fork()).join();
    }
    if (message.launchArguments !== undefined) {
      Struct.encode(Struct.wrap(message.launchArguments), writer.uint32(58).fork()).join();
    }
    if (message.launchContext !== undefined) {
      Struct.encode(Struct.wrap(message.launchContext), writer.uint32(66).fork()).join();
    }
    if (message.fallbackText !== "") {
      writer.uint32(74).string(message.fallbackText);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RuntimeLaunchPayload {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRuntimeLaunchPayload();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.extensionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.extensionPath = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.entrypointPath = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.environment = EnvironmentSnapshot.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.desktopContext = DesktopContextSnapshot.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.preferenceValues = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.launchArguments = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.launchContext = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.fallbackText = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RuntimeLaunchPayload {
    return {
      extensionId: isSet(object.extensionId)
        ? globalThis.String(object.extensionId)
        : isSet(object.extension_id)
        ? globalThis.String(object.extension_id)
        : "",
      extensionPath: isSet(object.extensionPath)
        ? globalThis.String(object.extensionPath)
        : isSet(object.extension_path)
        ? globalThis.String(object.extension_path)
        : "",
      entrypointPath: isSet(object.entrypointPath)
        ? globalThis.String(object.entrypointPath)
        : isSet(object.entrypoint_path)
        ? globalThis.String(object.entrypoint_path)
        : "",
      environment: isSet(object.environment) ? EnvironmentSnapshot.fromJSON(object.environment) : undefined,
      desktopContext: isSet(object.desktopContext)
        ? DesktopContextSnapshot.fromJSON(object.desktopContext)
        : isSet(object.desktop_context)
        ? DesktopContextSnapshot.fromJSON(object.desktop_context)
        : undefined,
      preferenceValues: isObject(object.preferenceValues)
        ? object.preferenceValues
        : isObject(object.preference_values)
        ? object.preference_values
        : undefined,
      launchArguments: isObject(object.launchArguments)
        ? object.launchArguments
        : isObject(object.launch_arguments)
        ? object.launch_arguments
        : undefined,
      launchContext: isObject(object.launchContext)
        ? object.launchContext
        : isObject(object.launch_context)
        ? object.launch_context
        : undefined,
      fallbackText: isSet(object.fallbackText)
        ? globalThis.String(object.fallbackText)
        : isSet(object.fallback_text)
        ? globalThis.String(object.fallback_text)
        : "",
    };
  },

  toJSON(message: RuntimeLaunchPayload): unknown {
    const obj: any = {};
    if (message.extensionId !== "") {
      obj.extensionId = message.extensionId;
    }
    if (message.extensionPath !== "") {
      obj.extensionPath = message.extensionPath;
    }
    if (message.entrypointPath !== "") {
      obj.entrypointPath = message.entrypointPath;
    }
    if (message.environment !== undefined) {
      obj.environment = EnvironmentSnapshot.toJSON(message.environment);
    }
    if (message.desktopContext !== undefined) {
      obj.desktopContext = DesktopContextSnapshot.toJSON(message.desktopContext);
    }
    if (message.preferenceValues !== undefined) {
      obj.preferenceValues = message.preferenceValues;
    }
    if (message.launchArguments !== undefined) {
      obj.launchArguments = message.launchArguments;
    }
    if (message.launchContext !== undefined) {
      obj.launchContext = message.launchContext;
    }
    if (message.fallbackText !== "") {
      obj.fallbackText = message.fallbackText;
    }
    return obj;
  },

  create(base?: DeepPartial<RuntimeLaunchPayload>): RuntimeLaunchPayload {
    return RuntimeLaunchPayload.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RuntimeLaunchPayload>): RuntimeLaunchPayload {
    const message = createBaseRuntimeLaunchPayload();
    message.extensionId = object.extensionId ?? "";
    message.extensionPath = object.extensionPath ?? "";
    message.entrypointPath = object.entrypointPath ?? "";
    message.environment = (object.environment !== undefined && object.environment !== null)
      ? EnvironmentSnapshot.fromPartial(object.environment)
      : undefined;
    message.desktopContext = (object.desktopContext !== undefined && object.desktopContext !== null)
      ? DesktopContextSnapshot.fromPartial(object.desktopContext)
      : undefined;
    message.preferenceValues = object.preferenceValues ?? undefined;
    message.launchArguments = object.launchArguments ?? undefined;
    message.launchContext = object.launchContext ?? undefined;
    message.fallbackText = object.fallbackText ?? "";
    return message;
  },
};

function createBasePingRequest(): PingRequest {
  return {};
}

export const PingRequest: MessageFns<PingRequest> = {
  encode(_: PingRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PingRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePingRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): PingRequest {
    return {};
  },

  toJSON(_: PingRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<PingRequest>): PingRequest {
    return PingRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<PingRequest>): PingRequest {
    const message = createBasePingRequest();
    return message;
  },
};

function createBasePingResponse(): PingResponse {
  return { ok: false };
}

export const PingResponse: MessageFns<PingResponse> = {
  encode(message: PingResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PingResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePingResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PingResponse {
    return { ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false };
  },

  toJSON(message: PingResponse): unknown {
    const obj: any = {};
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    return obj;
  },

  create(base?: DeepPartial<PingResponse>): PingResponse {
    return PingResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PingResponse>): PingResponse {
    const message = createBasePingResponse();
    message.ok = object.ok ?? false;
    return message;
  },
};

function createBaseStartDevSessionRequest(): StartDevSessionRequest {
  return { extensionId: "" };
}

export const StartDevSessionRequest: MessageFns<StartDevSessionRequest> = {
  encode(message: StartDevSessionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.extensionId !== "") {
      writer.uint32(10).string(message.extensionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartDevSessionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartDevSessionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.extensionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StartDevSessionRequest {
    return {
      extensionId: isSet(object.extensionId)
        ? globalThis.String(object.extensionId)
        : isSet(object.extension_id)
        ? globalThis.String(object.extension_id)
        : "",
    };
  },

  toJSON(message: StartDevSessionRequest): unknown {
    const obj: any = {};
    if (message.extensionId !== "") {
      obj.extensionId = message.extensionId;
    }
    return obj;
  },

  create(base?: DeepPartial<StartDevSessionRequest>): StartDevSessionRequest {
    return StartDevSessionRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StartDevSessionRequest>): StartDevSessionRequest {
    const message = createBaseStartDevSessionRequest();
    message.extensionId = object.extensionId ?? "";
    return message;
  },
};

function createBaseStartDevSessionResponse(): StartDevSessionResponse {
  return { ok: false };
}

export const StartDevSessionResponse: MessageFns<StartDevSessionResponse> = {
  encode(message: StartDevSessionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartDevSessionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartDevSessionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StartDevSessionResponse {
    return { ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false };
  },

  toJSON(message: StartDevSessionResponse): unknown {
    const obj: any = {};
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    return obj;
  },

  create(base?: DeepPartial<StartDevSessionResponse>): StartDevSessionResponse {
    return StartDevSessionResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StartDevSessionResponse>): StartDevSessionResponse {
    const message = createBaseStartDevSessionResponse();
    message.ok = object.ok ?? false;
    return message;
  },
};

function createBaseRefreshDevSessionRequest(): RefreshDevSessionRequest {
  return { extensionId: "" };
}

export const RefreshDevSessionRequest: MessageFns<RefreshDevSessionRequest> = {
  encode(message: RefreshDevSessionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.extensionId !== "") {
      writer.uint32(10).string(message.extensionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RefreshDevSessionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRefreshDevSessionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.extensionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RefreshDevSessionRequest {
    return {
      extensionId: isSet(object.extensionId)
        ? globalThis.String(object.extensionId)
        : isSet(object.extension_id)
        ? globalThis.String(object.extension_id)
        : "",
    };
  },

  toJSON(message: RefreshDevSessionRequest): unknown {
    const obj: any = {};
    if (message.extensionId !== "") {
      obj.extensionId = message.extensionId;
    }
    return obj;
  },

  create(base?: DeepPartial<RefreshDevSessionRequest>): RefreshDevSessionRequest {
    return RefreshDevSessionRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RefreshDevSessionRequest>): RefreshDevSessionRequest {
    const message = createBaseRefreshDevSessionRequest();
    message.extensionId = object.extensionId ?? "";
    return message;
  },
};

function createBaseRefreshDevSessionResponse(): RefreshDevSessionResponse {
  return { ok: false };
}

export const RefreshDevSessionResponse: MessageFns<RefreshDevSessionResponse> = {
  encode(message: RefreshDevSessionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RefreshDevSessionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRefreshDevSessionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RefreshDevSessionResponse {
    return { ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false };
  },

  toJSON(message: RefreshDevSessionResponse): unknown {
    const obj: any = {};
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    return obj;
  },

  create(base?: DeepPartial<RefreshDevSessionResponse>): RefreshDevSessionResponse {
    return RefreshDevSessionResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RefreshDevSessionResponse>): RefreshDevSessionResponse {
    const message = createBaseRefreshDevSessionResponse();
    message.ok = object.ok ?? false;
    return message;
  },
};

function createBaseStopDevSessionRequest(): StopDevSessionRequest {
  return { extensionId: "" };
}

export const StopDevSessionRequest: MessageFns<StopDevSessionRequest> = {
  encode(message: StopDevSessionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.extensionId !== "") {
      writer.uint32(10).string(message.extensionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StopDevSessionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStopDevSessionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.extensionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StopDevSessionRequest {
    return {
      extensionId: isSet(object.extensionId)
        ? globalThis.String(object.extensionId)
        : isSet(object.extension_id)
        ? globalThis.String(object.extension_id)
        : "",
    };
  },

  toJSON(message: StopDevSessionRequest): unknown {
    const obj: any = {};
    if (message.extensionId !== "") {
      obj.extensionId = message.extensionId;
    }
    return obj;
  },

  create(base?: DeepPartial<StopDevSessionRequest>): StopDevSessionRequest {
    return StopDevSessionRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StopDevSessionRequest>): StopDevSessionRequest {
    const message = createBaseStopDevSessionRequest();
    message.extensionId = object.extensionId ?? "";
    return message;
  },
};

function createBaseStopDevSessionResponse(): StopDevSessionResponse {
  return { ok: false };
}

export const StopDevSessionResponse: MessageFns<StopDevSessionResponse> = {
  encode(message: StopDevSessionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StopDevSessionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStopDevSessionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StopDevSessionResponse {
    return { ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false };
  },

  toJSON(message: StopDevSessionResponse): unknown {
    const obj: any = {};
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    return obj;
  },

  create(base?: DeepPartial<StopDevSessionResponse>): StopDevSessionResponse {
    return StopDevSessionResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StopDevSessionResponse>): StopDevSessionResponse {
    const message = createBaseStopDevSessionResponse();
    message.ok = object.ok ?? false;
    return message;
  },
};

function createBaseShutdownEvent(): ShutdownEvent {
  return {};
}

export const ShutdownEvent: MessageFns<ShutdownEvent> = {
  encode(_: ShutdownEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShutdownEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShutdownEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ShutdownEvent {
    return {};
  },

  toJSON(_: ShutdownEvent): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ShutdownEvent>): ShutdownEvent {
    return ShutdownEvent.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ShutdownEvent>): ShutdownEvent {
    const message = createBaseShutdownEvent();
    return message;
  },
};

function createBasePopViewEvent(): PopViewEvent {
  return {};
}

export const PopViewEvent: MessageFns<PopViewEvent> = {
  encode(_: PopViewEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PopViewEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePopViewEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): PopViewEvent {
    return {};
  },

  toJSON(_: PopViewEvent): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<PopViewEvent>): PopViewEvent {
    return PopViewEvent.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<PopViewEvent>): PopViewEvent {
    const message = createBasePopViewEvent();
    return message;
  },
};

function createBasePreferencesChangedEvent(): PreferencesChangedEvent {
  return { values: undefined };
}

export const PreferencesChangedEvent: MessageFns<PreferencesChangedEvent> = {
  encode(message: PreferencesChangedEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.values !== undefined) {
      Struct.encode(Struct.wrap(message.values), writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PreferencesChangedEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePreferencesChangedEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.values = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PreferencesChangedEvent {
    return { values: isObject(object.values) ? object.values : undefined };
  },

  toJSON(message: PreferencesChangedEvent): unknown {
    const obj: any = {};
    if (message.values !== undefined) {
      obj.values = message.values;
    }
    return obj;
  },

  create(base?: DeepPartial<PreferencesChangedEvent>): PreferencesChangedEvent {
    return PreferencesChangedEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PreferencesChangedEvent>): PreferencesChangedEvent {
    const message = createBasePreferencesChangedEvent();
    message.values = object.values ?? undefined;
    return message;
  },
};

function createBaseRuntimeEvent(): RuntimeEvent {
  return { shutdown: undefined, popView: undefined, preferencesChanged: undefined };
}

export const RuntimeEvent: MessageFns<RuntimeEvent> = {
  encode(message: RuntimeEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.shutdown !== undefined) {
      ShutdownEvent.encode(message.shutdown, writer.uint32(10).fork()).join();
    }
    if (message.popView !== undefined) {
      PopViewEvent.encode(message.popView, writer.uint32(18).fork()).join();
    }
    if (message.preferencesChanged !== undefined) {
      PreferencesChangedEvent.encode(message.preferencesChanged, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RuntimeEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRuntimeEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.shutdown = ShutdownEvent.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.popView = PopViewEvent.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.preferencesChanged = PreferencesChangedEvent.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RuntimeEvent {
    return {
      shutdown: isSet(object.shutdown) ? ShutdownEvent.fromJSON(object.shutdown) : undefined,
      popView: isSet(object.popView)
        ? PopViewEvent.fromJSON(object.popView)
        : isSet(object.pop_view)
        ? PopViewEvent.fromJSON(object.pop_view)
        : undefined,
      preferencesChanged: isSet(object.preferencesChanged)
        ? PreferencesChangedEvent.fromJSON(object.preferencesChanged)
        : isSet(object.preferences_changed)
        ? PreferencesChangedEvent.fromJSON(object.preferences_changed)
        : undefined,
    };
  },

  toJSON(message: RuntimeEvent): unknown {
    const obj: any = {};
    if (message.shutdown !== undefined) {
      obj.shutdown = ShutdownEvent.toJSON(message.shutdown);
    }
    if (message.popView !== undefined) {
      obj.popView = PopViewEvent.toJSON(message.popView);
    }
    if (message.preferencesChanged !== undefined) {
      obj.preferencesChanged = PreferencesChangedEvent.toJSON(message.preferencesChanged);
    }
    return obj;
  },

  create(base?: DeepPartial<RuntimeEvent>): RuntimeEvent {
    return RuntimeEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RuntimeEvent>): RuntimeEvent {
    const message = createBaseRuntimeEvent();
    message.shutdown = (object.shutdown !== undefined && object.shutdown !== null)
      ? ShutdownEvent.fromPartial(object.shutdown)
      : undefined;
    message.popView = (object.popView !== undefined && object.popView !== null)
      ? PopViewEvent.fromPartial(object.popView)
      : undefined;
    message.preferencesChanged = (object.preferencesChanged !== undefined && object.preferencesChanged !== null)
      ? PreferencesChangedEvent.fromPartial(object.preferencesChanged)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

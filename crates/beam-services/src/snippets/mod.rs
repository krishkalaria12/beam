pub(crate) mod config;
pub mod error;
pub mod model;

// PORT: apps/desktop/src-tauri/src/snippets/mod.rs (partial)
// The model lands with the state module that needs it; db, repository,
// helpers, runtime and the service functions convert with lane A3.
